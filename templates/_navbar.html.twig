<nav class="navbar navbar-expand-lg bg-dark">
    <div class="container-fluid">
        <div>
            <a href="{{ path('app_index') }}"><img src="{{ asset('build/images/logo.png') }}" alt=""/></a>
            <a class="navbar-brand ws-nav ws-nav-item px-5 text-center" href="{{ path('app_index') }}">Wild Séries</a>
        </div>
        <div>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item dropdown px-4">
                        <a class="nav-link dropdown-toggle ws-nav-item" href="" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Catégories
                        </a>
                        <ul class="dropdown-menu">
                            {% for category in allCategories.findAll %}
                            <li><a class="dropdown-item" href="{{ path('category_index') }}"> {{ category.name }}</a></li>
                            {% endfor %}
                        </ul>
                    </li>
                    <li class="nav-item dropdown px-4">
                        <a class="nav-link dropdown-toggle ws-nav-item" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Acteurs
                        </a>
                        <ul class="dropdown-menu">
                            {% for actor in allActors.findAll %}
                                <li><a class="dropdown-item" href="{{ path('actor_index') }}"> {{ actor.name }}</a></li>
                            {% endfor %}
                        </ul>
                    </li>
                    <li class="nav-item px-4">
                        <a class="nav-link ws-nav-item" href="#">Mes favoris</a>
                    </li>
                    <li class="nav-item px-4">
                        <a class="nav-link ws-nav-item" href="#">Inscription</a>
                    </li>
                    <li class="nav-item px-4">
                        <a class="nav-link ws-nav-item" href="#">Connexion</a>
                    </li>
                </ul>
                <!--
                <form class="d-flex" role="search">
                    <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success" type="submit">Search</button>
                </form>
                -->
            </div>
        </div>
    </div>
</nav>
