<nav class="navbar navbar-expand-lg bg-dark">
    <div class="container-fluid">
        <div>
            <a href="{{ path('app_index') }}"><img src="{{ asset('build/images/logo.png') }}" alt=""/></a>
            <a class="navbar-brand ws-nav ws-nav-item px-5 text-center" href="{{ path('app_index') }}">Wild Séries</a>
        </div>
        <div>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    {{ component('navbar') }}

                    <li class="nav-item px-4">
                        <a class="nav-link ws-nav-item" href="#">Mes favoris</a>
                    </li>

                    <li class="nav-item px-4">
                        {% if app.user %}
                            <a class="nav-link ws-nav-item" href="{{ path('user_profile', {id: app.user.id}) }}">Mon compte</a>
                        {% else %}
                            <a class="nav-link ws-nav-item" href="{{ path('app_register') }}">Inscription</a>
                        {% endif %}
                    </li>

                    <li class="nav-item px-4">
                        {% if app.user %}
                            <a class="nav-link ws-nav-item" href="{{ path('app_logout') }}">Déconnexion</a>
                        {% else %}
                            <a class="nav-link ws-nav-item" href="{{ path('app_login') }}">Connexion</a>
                        {% endif %}
                    </li>
                </ul>
                <!--
                <form class="d-flex" role="search">
                    <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success" type="submit">Search</button>
                </form>
                -->
            </div>
        </div>
    </div>
</nav>
