<div class="d-flex"{{ attributes }}>
    <li class="nav-item dropdown px-4">
        <a class="nav-link dropdown-toggle ws-nav-item" href="" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Catégories
        </a>
        <ul class="dropdown-menu">
            {% for category in this.categories %}
                <li><a class="dropdown-item" href="{{ path('category_show', {categoryName: category.name}) }}"> {{ category.name }}</a></li>
            {% endfor %}
        </ul>
    </li>
    <li class="nav-item dropdown px-4">
        <a class="nav-link dropdown-toggle ws-nav-item" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Acteurs
        </a>
        <ul class="dropdown-menu">
            {% for actor in this.actors %}
                <li><a class="dropdown-item" href="{{ path('actor_show', {slug: actor.slug}) }}"> {{ actor.name }}</a></li>
            {% endfor %}
        </ul>
    </li>
</div>
